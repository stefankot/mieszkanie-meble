node_modules/
dist-edytor/
# edytor-app/ zapisuje wyłącznie workflow edytor.yml na main
edytor-app/
.DS_Store
# Klucze API tylko lokalnie — nigdy do repo
.env
.env.*
!.env.example
# Mapy wyciągnięte z Blendkitu + KTX2 (narzedzia/blendkit.mjs). Domyślnie poza gitem: to pliki binarne,
# a licencja Blendkit (royalty_free) zabrania redystrybucji. Publikujemy wyłącznie materiały CC0 —
# każdy dopisujemy tu wyjątkiem, żeby opublikowana strona miała swoją bibliotekę.
tekstury/*
!tekstury/katalog.json
!tekstury/wood-floor/
!tekstury/wood-floor-broken/
